# RoyalStroje -- Decision Log

### 2026-07-02 -- Session 34
**Decision:** Swapped the body font **Manrope -> Source Sans 3** site-wide (Archivo display stays), and **removed the availability state from catalog product cards entirely** (no "Momentalne nedostupne" badge, no grayscale, "Zavolat" CTA on every card). Also: functional catalog controls (FO/PO toggle, search, subcategory chips) now render **without reveal animations**, and the ProductDetail page was redesigned around a corrected title hierarchy (H1 = descriptive product name, brand as kicker) with CTAs moved into the price card.
**Why:** Owner reported body text was hard to read ("medzery medzi slovami su velmi male") -- Manrope has an unusually narrow word-space glyph and semi-closed geometric letterforms that set Slovak (long words, heavy diacritics) cramped; Source Sans 3 is a humanist screen-reading workhorse with wider word spaces and open apertures, and pairs with Archivo on a proper contrast axis (grotesque display + humanist body). Availability removal is a business call by the owner: an unavailable-looking card kills the call, and a phone call is the conversion -- the owner would rather take the call and offer an alternative/date. Reveal removal on filter controls follows from principle: functional controls must never sit at opacity 0, and the opacity-transition layer in exactly that zone was implicated in the budget-Android tile corruption. ProductDetail's H1 previously showed the brand ("Festa") while the eyebrow held the real product name -- backwards for both users and scanning.
**Alternatives:** (B) Fix readability via letter/word-spacing on Manrope -- rejected, treats the symptom; the space glyph is the font's design. (C) Inter/DM Sans as body -- rejected deliberately (generic-AI default fonts; Inter's word space is nearly as tight as Manrope's). (D) Keep availability badge but soften it -- owner chose full removal. **Notes:** the scrambled GPU band the owner saw on staging (Xiaomi) turned out to be the **Vercel Toolbar** (fixed element + backdrop-filter = the exact [[mobile-gpu-garbage-bug]] trigger class) -- confirmed clean in a logged-out browser; production is unaffected. Zoom seam on card images fixed with a base `scale-[1.02]` overdraw (static 2D transform = no persistent GPU layer, guard upheld). PRODUCT.md type section updated. Commits `65cd1b2` + `7925e08` on `dev`, `main` untouched.

### 2026-07-02 -- Session 33
**Decision:** After an impeccable design audit (scored 27/40) of the new dark-on-light redesign, **refine the catalog within the dark-card direction rather than flip product cards to light**, and scope the polish pass to **homepage + catalog only** (subpages deferred). Core moves: one filled-orange CTA per component (catalog cards were orange badge + orange button + orange call all at once), thin the eyebrow labels down to only where they carry information (they sat above nearly every section), collapse the 4 identical `01–04`-watermark "why us" cards into a single hairline-divided panel, and harden the reveal animations so content is visible without JS (`html.js-reveal` gate + print fallback).
**Why:** Owner asked to make the site "more professional." The audit diagnosis was **volume control**, not a broken foundation — orange on everything and every component a heavy dark island flattened hierarchy, and the eyebrow/`01–04` scaffolding read as an AI template. The dark-card catalog was an owner-approved session-30 decision, so refining within it (quieter, one-orange-per-moment) preserves that call while fixing the hierarchy; flipping to light cards would have re-litigated a settled choice. Homepage+catalog is where the money path and the worst hierarchy problems live, so it's the highest-leverage scope for one pass. Reveal hardening was prompted by headless capture showing FAQ/QuoteForm/Blog CTA as a blank band — the class-gated hide was a genuine content-visibility risk for crawlers/print.
**Alternatives:** (B) Flip product cards to white on `#FAFAFA` (rental-industry-standard look, Kärcher/Boels) — offered with an ASCII mockup, owner chose "refine dark cards." (C) Whole-site scope in one pass — offered, owner chose homepage+catalog first (shared primitives still improved, so subpages partially benefit). (D) Keep eyebrows as brand grammar / redesign a new section-header device — owner chose "thin them out." **Constraints upheld:** dark-on-light theme from [[homepage-theme-dark-on-light]] unchanged; GPU guard from [[mobile-gpu-garbage-bug]] (no `backdrop-filter` on fixed/sticky, reveal end-states stay `transform: none`); all texts/CTAs/prices preserved. Also fixed a stale hero preload (120 KB unused per visit). 5 commits `f5d83f0`→`518ec12` + docs on `dev`, `main` untouched. New `PRODUCT.md` captures the design context for future passes.

### 2026-06-30 -- Session 29
**Decision:** Stood up a **dev/staging environment using Vercel preview deployments on a new long-lived `dev` branch**, rather than a separate hosting/Vercel project or Supabase branching. `main` stays the Vercel Production Branch (→ royalstroje.sk); every other branch auto-deploys to a Preview URL, so `dev` is fully isolated from production. Chose the **auto Vercel preview URL** (declined a `dev.royalstroje.sk` subdomain) and **web-only** scope (dashboard excluded, since it writes to the DB). First use: committed the entire uncommitted sessions-24→28 redesign + today's new "Zoženieme akýkoľvek stroj" service as the `dev` baseline (`577098e`), then the homepage "Nenašli ste stroj?" CTA band (`95a1481`) — both pushed to `origin/dev`, `main` untouched. Also replaced the Royal Fleet service with a partner-sourcing service named **"Zoženieme akýkoľvek stroj"** (owner-picked from 5 proposed names) and added the dark+orange homepage CTA linking to it.
**Why:** Owner had ~5 sessions of redesign stuck uncommitted because pushing to `main` auto-deploys live, so there was no safe place to push work, get owner review, or do the long-deferred real-Android GPU test. Vercel preview deployments already exist on their plan, need no extra infra or cost, and give per-branch isolated URLs — the lowest-friction fix that directly unblocks the review workflow (push `dev` → review on staging → merge `dev`→`main` to ship). Auto URL + web-only keeps setup near-zero (no DNS, no second Supabase); the public site is read-heavy so sharing the prod Supabase is safe. "Zoženieme akýkoľvek stroj" was chosen over "Sprostredkovanie prenájmu" / "Technika bez obmedzení" etc. for maximum from-the-name clarity.
**Alternatives:** (B) `dev.royalstroje.sk` stable subdomain — offered, owner chose the auto Vercel URL (no DNS work). (C) Separate Supabase project for staging — deferred; only needed once the dashboard (writes) goes on staging. (D) Per-PR/feature-branch previews instead of a named `dev` branch — viable, but a single `dev` line is simpler for one owner-reviewer. (E) Commit the redesign straight to `main` — rejected; that is the exact "commit = deploy to prod" problem the dev env removes. **Notes:** `.env` confirmed gitignored before pushing (no secrets leaked). Owner-side Vercel steps still pending: enable env vars for the **Preview** environment (else staging Supabase + forms break) + confirm Production Branch = `main`; reCAPTCHA/EmailJS are domain-restricted so the form may error on the `*.vercel.app` preview domain unless whitelisted. GPU guard from [[mobile-gpu-garbage-bug]] upheld on the new dark `SourcingBanner` (decor is non-fixed `absolute`; reveal ends at `transform: none`). `RoyalFleet.jsx` deleted (only referenced from the 3 files updated). Captured in the `dev-staging-environment` memory.

### 2026-06-26 -- Session 28
**Decision:** Rolled the light `PageHero` component out to the **11 remaining dark subpage heroes** (Blog, BlogDetail, Kontakt, CenovaPonuka, NahradneDiely, RoyalFleet, SkoLenieObsluhy, Partneri, PredajTechniky, ServisNaradia, ZemnePrace), replacing each old dark `<section>` photo+black-overlay hero **and** its trailing `<hr>` orange seam with one `<PageHero>` (which renders its own bottom rule). Props mapped verbatim from each old hero (eyebrow/title/subtitle/image), CTAs standardized to `.btn-primary` "Zavolať teraz" + `.btn-outline-light` "Zobraziť techniku" to match Sluzby. Executed as **10 parallel subagents** (one per uniform file) + **BlogDetail by hand** (dynamic: meta→`chips`, excerpt→subtitle, back-link→`actions`). **ProductDetail left untouched** (its hero is a 280px product photo banner, not a text hero). Left **UNCOMMITTED** at owner's request (stacks on the session-24→27 WIP).
**Why:** Session 27 added `PageHero` but only Sluzby + DovozTechniky used it, so the site was half-converted — owner explicitly asked for the light hero "ako v Sluzbach" on all remaining pages. The conversion is highly uniform (identical dark-hero markup across pages), making it an ideal parallel-agent fan-out against a tight spec; BlogDetail's dynamic meta/back-button warranted a hand edit. Standardizing CTAs gives every subpage hero the same call-to-action as the reference. Not committing because push to `main` auto-deploys live and the whole redesign (sessions 24–28) is meant to ship as one decision.
**Alternatives:** (B) Hand-edit all 11 serially — rejected; slower and error-prone vs. a verified agent fan-out for identical markup. (C) Also convert ProductDetail's photo banner to `PageHero` — rejected; it's a different component (product image strip), not the text-hero pattern; flagged for the owner instead. (D) Add a secondary CTA per-page tuned to context — rejected in favour of one uniform CTA pair matching Sluzby. **Constraints upheld:** orange `#FF6600` unchanged; mobile untouched (`PageHero` is `hidden md:block`, each page keeps its compact mobile heading); GPU guard from [[mobile-gpu-garbage-bug]] (PageHero's orange glow is a non-fixed `absolute` div; no `backdrop-filter` on `fixed`/`sticky`); removed every now-unused `heroRef` + `GraduationCap`; build + lint clean (caught + fixed one stray `heroInView` in BlogDetail's mobile header).

### 2026-06-24 -- Session 26
**Decision:** Rolled the session-25 light theme out to **every remaining public page** (18 files) + the shared `ContactForm.jsx`, as a visual-only restyle. Heroes/header/footer stay dark; only content below each hero converts to `#FAFAFA` + `.card-light`, with each hero's bottom fade flipped `#181818`→`#FAFAFA` to kill the seam. Owner chose **"všetko svetlé"** — even the service pages' `bg-black` CTA bands and animated orange-glow backgrounds were converted to light (glow `@keyframes` removed), so there are no dark accent zones. Executed via a `Workflow` (one agent per page: convert → adversarially verify → auto-fix); `PredajTechniky` was hand-finished after its agent hit an API timeout. Left **UNCOMMITTED** at owner's request (stacks on the session-24/25 Home WIP).
**Why:** After session 25 only Home was light, so the site looked half-converted — the owner explicitly wanted one unified design across the whole web. The design system already existed (`.card-light`/`.input-light`/`.btn-outline-light`, `ContentSection light`, `CustomSelect light`), so this was a mechanical rollout, not new design — ideal for parallel agents with adversarial verification. Converting `ContactForm` outright (rather than a dark/light dual variant) is safe because both pages that render it (Kontakt + Cenová ponuka) are now light, which also undoes session 25's deliberate "keep Kontakt form dark" call. Not committing because push to `main` auto-deploys live and the split hero on Home is still WIP.
**Alternatives:** (B) Keep dark CTA/glow accent bands for contrast — offered as an option, owner rejected in favour of full light. (C) Hand-edit all 18 pages serially — rejected; slower and error-prone vs. a verified agent fan-out. (D) A dual light/dark `ContactForm` — unnecessary since no page keeps it dark now. **Discovery:** `ServisNaradia`/`DovozTechniky`/`ZemnePrace` are orphan files (no route in `App.jsx`, not linked) — converted for consistency but flagged for the owner to delete or wire up. **Constraints upheld:** orange `#FF6600` unchanged; GPU guard from [[mobile-gpu-garbage-bug]] (no `backdrop-filter` on `fixed`/`sticky`; Kontakt lightbox blur gated `md:`; `.reveal*.in-view` left at `transform: none`); build + lint clean; adversarial grep found only legitimate stay-dark surfaces (heroes, photo overlays, lightbox).

### 2026-06-23 -- Session 25
**Decision:** Made the homepage light **Home-only via a `light` prop on the shared `ContentSection`** (default stays `#181818`; only `Catalog`/Home passes `light` → `#FAFAFA`), then did a **full "clean white panels" redesign** of every homepage surface — not just a background flip. Added three reusable light primitives in `index.css` (`.card-light`, `.input-light`, **a new `.btn-outline-light`** rather than mutating `.btn-secondary`). Shared components use opt-in light variants: `CustomSelect` gained a `light` prop so the dark Kontakt `ContactForm` stays untouched; product price/badge pills deliberately kept dark over photos for legibility. Kept **UNCOMMITTED** at owner's request (continues next session, ships together with the session-24 split hero).
**Why:** Owner's examples were all on Home, and flipping the `ContentSection` default would cascade light onto 12 other pages (huge surface + regression risk) — a `light` prop scopes it cleanly to Home. A background-only flip left every dark card/pill/accordion floating as black blocks on white (looked unfinished), exactly what the owner flagged, so a surface-level redesign was required for a professional result. New primitives avoid breaking `.btn-secondary` (white-on-translucent, used by the dark hero) and the dark `CustomSelect` (shared with Kontakt). Not committing because pushing to `main` auto-deploys live royalstroje.sk, and the split hero on the same page is still WIP.
**Alternatives:** (B) Flip `ContentSection` default to light site-wide — rejected; out of scope, ~13 pages of text/contrast auditing, high regression risk. (C) Background-only flip keeping dark cards — rejected; that IS the floating-black-blocks problem. (D) Globally recolor `CustomSelect`/`.btn-secondary` — rejected; breaks the dark Kontakt page + dark hero. **Constraints upheld:** orange `#FF6600` unchanged; GPU guard from [[mobile-gpu-garbage-bug]] (no new `backdrop-filter` on any `fixed`/`sticky`; dropped the 3 ProductCard badge blurs; all `.reveal*.in-view` left at `transform: none`).

### 2026-06-23 -- Session 24
**Decision:** Built the requested split-screen hero redesign as a **brand-new component (`HeroSplit.jsx`)** swapped into `Home.jsx`, with the old `Hero.jsx` + `MobileHero.jsx` **left untouched on disk** and their import/render block **commented out** (revert = uncomment + remove `<HeroSplit/>`). The new hero is a single responsive component: desktop = diagonal 50/43 split (Prenájom \| Predaj) made **full-screen** via `md:h-screen md:flex md:flex-col` with the split as `flex-1` (absorbs leftover so the bottom orange hairline lands exactly at the 1920×1080 viewport bottom); mobile = stacked cards. Below the split: a 4-image **16:9** strip (mock = existing repo photos) framed top+bottom by orange hairlines, then a **white** USP band (dark text, orange icons). Kept **UNCOMMITTED / undeployed** at session end by owner's choice.
**Why:** Owner wanted to try a bolder hero but explicitly wanted a safe path back ("ulož aktuálnu hero sekciu aby sme sa k nej vedeli vrátiť") and to keep iterating across sessions. A separate component + commented-out old block makes revert a 2-line flip with zero risk to the proven hero, and avoids a messy diff inside the live files. Not committing/pushing matters because **push to `main` auto-deploys to the live royalstroje.sk** — shipping an in-progress hero was not wanted; this mirrors the session-22 "leave UNCOMMITTED, verify first" precedent.
**Alternatives:** (B) Edit `Hero.jsx`/`MobileHero.jsx` in place — rejected; loses the clean revert and risks the live hero. (C) Commit locally without pushing (safety checkpoint) — offered, owner chose fully uncommitted to keep tweaking freely. (D) Commit + push to go live now — rejected; still iterating. **Constraints upheld:** GPU guard from [[mobile-gpu-garbage-bug]] (no `backdrop-filter` on any `fixed`/`sticky` element; in-flow gradient overlays for legibility; keyframes end at `transform: none`). **Gotcha fixed:** a CSS entrance animation with `animation-fill-mode: both` (final `transform: none`) sitting on a `.btn-primary` link **overrode its `:hover` lift** — moved the animation onto a wrapper `div` so the button keeps its hover translateY. Also: owner's other local project "ROYAL WORKS" holds Vite port 5173, so RoyalStroje dev must run on another port (used 5191).

### 2026-06-16 -- Session 23
**Decision:** Rolled the session-22 industrial-premium design system out to **every remaining public page** (18 files: Sluzby, Kontakt, Partneri, CenovaPonuka, Kosik, Blog, BlogDetail, ProductDetail, the 7 service pages, and the 3 legal pages) as a visual-only restyle. Standardized on: `.eyebrow` labels above hero + section headings, `.btn-primary`/`.btn-secondary` for all CTAs (dropping the old `rounded-full` orange pills), one canonical card style (`from-zinc-900 to-zinc-950` + restrained `hover:shadow-orange-primary/15`), orange top-accent rules on feature cards, and removal of the loud `border-2 orange/30 + hover:scale-[1.02] + -translate-y-2 + shadow-2xl` cards plus the animated `gradient-shift` sheen overlays. Shipped as **two commits** (session-22 landing `bf57e7d`, session-23 page rollout `f8d1d38`) to preserve the documented session boundary.
**Why:** Session 22's global font change already shifted every page visually, so the pages were half-restyled (new type, old component styling) and looked inconsistent. Finishing the rollout is the consistency payoff and is cheap because the primitives already existed. The restrained card style reads more "engineered/premium" than the previous glow-heavy cards and reduces GPU surface area on mobile. Used 4 parallel subagents for the mechanical service/legal pages (distinct files → no conflicts) while hand-editing the judgment-heavy pages, against a tight written spec including the GPU rules.
**Alternatives:** (B) Leave the secondary pages on the old styling — rejected; the font change made the mix look unfinished. (C) One squashed commit for sessions 22+23 — rejected in favour of two commits matching the session log. **Constraint upheld:** no new `backdrop-filter` on any `fixed`/`sticky` element (Kontakt lightbox blur gated to `md:`, no-op in-flow blur dropped from Kosik); all `.reveal*.in-view` end states left at `transform: none`; only restrained/`-translate-y-1` hovers used — same GPU guard as [[mobile-gpu-garbage-bug]] / session 21.

### 2026-06-16 -- Session 22
**Decision:** Landing-page redesign committed to an **industrial-premium** direction with an **industrial web-font pairing** -- **Archivo** (display/headings) + **Manrope** (body) loaded from Google Fonts (`latin-ext` subset, `display=swap`, preconnect) -- plus a small set of reusable CSS primitives (`.eyebrow`, `.hairline`, `.btn-primary`, `.btn-secondary`). Dark theme, orange `#FF6600`, white secondary, hero image and all texts/CTAs were kept; the catalog's data/filter/cart logic was left untouched (visual-only restyle).
**Why:** The site previously used **system default fonts (no web font at all)**, so typography was the single highest-impact modernizer within the owner's "keep dark + orange + hero + copy" constraints. An industrial/utilitarian aesthetic fits an equipment-rental brand for builders and reads as professional/trustworthy rather than flashy. Centralising eyebrow/hairline/button styling as primitives keeps every section visually consistent and cheap to extend. Font pairing chosen over keeping system fonts (the `frontend-design` skill explicitly steers away from system/Inter) and over a single sans (a display+body pair gives stronger hierarchy).
**Alternatives:** (B) Keep system fonts and polish only spacing/contrast -- rejected as too small a change for the requested "modernejší vzhľad". (C) Bolder/dramatic direction (bigger type, glow, grid-breaking layouts) -- rejected as less credible for the audience. (D) Marketing-sections-only scope leaving the catalog -- rejected; owner wanted the whole page. **Constraint note:** why-us cards use **non-transform hover only** (border/shadow/accent-line width) instead of `hover:scale`/`-translate`, because permanently/animated transforms on `.reveal` elements fight the `transform: none` de-promotion from session 21 and risk re-triggering the budget-Android GPU garbage; product-card hover keeps a tiny `-translate-y-1` (only the hovered card promotes). All hero/panel `backdrop-blur` is on `relative` (non-fixed) elements, so it does not re-introduce the session-21 fixed-element trigger.

### 2026-06-03 -- Session 21
**Decision:** Fixed the mobile GPU garbage bands by removing `backdrop-filter` from fixed/sticky on-screen elements (hamburger button + its overlay, CookieBanner gated to desktop), disabling `AnimatedBackground` on mobile, and de-promoting `reveal` animations (`transform: none`) -- rather than refactoring the FAQ into a separate simplified mobile-only section.
**Why:** The scrambled GPU garbage / scroll checkerboarding only reproduced on real budget Android (Xiaomi/Mali), never in Chrome DevTools mobile-view. Root cause was `backdrop-filter` on the `fixed` hamburger button, which forces Chromium to keep the page-tall content behind it composited as a read-back backdrop layer the GPU can't sustain during scroll. Removing the trigger fixes the actual cause at the source, keeps a single shared codebase for desktop/mobile, and is visually invisible (the affected backgrounds were already 90-95% opaque). General rule adopted: never put `backdrop-filter` on a `fixed`/`sticky` element that stays on screen during scroll on budget Android.
**Alternatives:** (B) Separate simplified mobile-only FAQ section (owner's suggestion) -- rejected because it only treats the symptom; the FAQ was just where the corruption surfaced, the trigger lived elsewhere (the global fixed hamburger), so the garbage would likely have reappeared. (C) The compositing-layer-size theories tried first -- `overflow-x-clip` / `overflow: visible` on root div / ContentSection / FAQ, and `translateZ` GPU promotion -- all failed (and `overflow: visible` broke the sticky sidebar layout); reverted. They missed the real trigger because the fixed-element backdrop-filter was forcing whole-page compositing regardless of container overflow.

### 2026-05-22 -- Session 18
**Decision:** Replaced native `<input type="date">` with `<input type="text" placeholder="DD.MM.YYYY">` for the new contact `birth_date` field, parsed via `isoToDmy` / `dmyToISO` helpers in `constants.js`.
**Why:** Native date inputs render with browser-locale-dependent placeholder formats ("11-Oct-yyyy" on English-locale browsers, "dd.mm.rrrr" on Slovak ones). Operator was confused by the inconsistent month-name display and wanted a numeric `DD.MM.YYYY` everywhere regardless of OS/browser. A controlled text input gives full format control and accepts `.`, `/`, `-`, space separators. Trade-off: lose the native date picker UI -- acceptable since the field is occasional and the format is short.
**Alternatives:** (B) Force browser locale via `lang="sk-SK"` attribute -- inconsistently respected across browsers and doesn't solve the placeholder issue. (C) Build a custom date picker component -- overkill for a few sporadic inputs; can revisit if there's broader date-entry UX work later.

### 2026-05-22 -- Session 18
**Decision:** Reports page monthly/yearly revenue cards switched from `invoices` table (filtered by `status='paid'` + `paid_at`) to `reservations` table (filtered by `status IN ('completed','invoiced','paid')` + `created_at`), with VAT subtracted (`Σ(total - vat_amount)`).
**Why:** The invoices-based query was returning 0 because the current workflow never marks invoices as `paid` -- deals go `inquiry → completed` and that's where the revenue lives. The Dashboard StatCard + Sidebar MiniStat were already using reservations and showing correct numbers; Reports was the outlier. Aligning all three eliminates the contradiction where the sidebar said 3 301 € and the Reports card said 0 €.
**Alternatives:** (B) Fix the workflow to actually mark invoices as paid before they show up in Reports -- requires user behaviour change and doesn't help retrospectively. (C) Show both "invoiced revenue" and "completed revenue" as separate cards -- more UI complexity for a metric the operator currently treats as one number.

### 2026-05-22 -- Session 18
**Decision:** Active24 hosting paket `fabacv0r_1` kept active after deleting WordPress (files + DB), solely to retain the email service for `info@royalstroje.sk`.
**Why:** Live website runs on Vercel (`royalstroje.sk` A record → 216.198.79.1), so the Active24 web stack was dead weight (378 MB old WordPress files + 23 MB DB filling the 1 GB quota). But MX records still route mail to `mx10/mx20.active24.cz`, which depends on the hosting paket being active. Deleting WordPress freed ~400 MB without breaking email continuity. Confirmed approach: delete via `webftp.royalstroje.sk` (Monsta file manager) + `dbadmin.royalstroje.sk` (PhpMyAdmin DROP database `QVceRP65aOtvb3zi`); never click "Odstrániť službu / Navždy smazat" on the domain attachment (would destroy mailboxes).
**Alternatives:** (B) Migrate emails to Google Workspace (~6€/user) or Zoho and cancel Active24 -- cheaper long-term if only 1-2 mailboxes, but requires MX migration + mail import + onboarding. Logged as priority 3 in handoff for future evaluation. (C) Keep WordPress in place -- no benefit, just risk of stale plugins becoming a security liability.

### 2026-05-04 -- Session 17
**Decision:** Cookie banner is info-only (single "Rozumiem" + close, no consent categories) for now -- to be expanded into a full Accept/Reject/Customize flow with Google Consent Mode v2 once GA4 is added.
**Why:** Site currently sets only strictly-necessary cookies (`_GRECAPTCHA` for bot protection + MDN chatbot which doesn't store user identifiers) and a few `localStorage` keys for cart/rate-limit/consent. Under SK ÚOOÚ guidance, strictly-necessary cookies don't require granular consent -- only transparent disclosure. Building a full consent UI and 12-month re-prompt logic before any analytics/marketing tooling exists would be over-engineering and would also lose ~30-50 % of analytics data prematurely.
**Alternatives:** (B) Build the full 3-category banner + Consent Mode v2 now and wire GA4 later -- more work upfront, harder to test correctly with no real analytics tag to gate. (C) Skip the banner entirely (legally defensible since only strictly-necessary cookies are set) -- but visible disclosure is the SK norm and the GDPR page already promises one. Option A is the smallest correct step that covers compliance now and leaves a clean upgrade path.

### 2026-05-04 -- Session 16
**Decision:** Dashboard moved to dedicated `app.royalstroje.sk` subdomain (kept `royal-stroje-dashboard.vercel.app` working in parallel, no redirect).
**Why:** Branded URL is what staff will share verbally and is easier to remember; matches the convention `web → royalstroje.sk`, `dashboard → app.royalstroje.sk`. Keeping the vercel.app URL alive avoids breaking any existing bookmarks or email links during transition.
**Alternatives:** (B) Set 308 permanent redirect from `*.vercel.app` to `app.royalstroje.sk` -- could break third-party integrations or bookmarks we haven't audited yet. (C) Use `dashboard.royalstroje.sk` -- longer to type and less consistent with the planned `portal.royalstroje.sk` for the Royal Card portal.

### 2026-04-30 -- Session 15
**Decision:** Ad-hoc reservation items implemented via nullable `equipment_id` + `custom_name` + `custom_rate_unit` columns on `reservation_items`, gated by a CHECK constraint requiring one or the other.
**Why:** Operator needs to put one-off machines on a contract without registering them in the permanent catalog. The line item itself carries the necessary identity, keeping the equipment table clean. PDF/UI fallback to `custom_name` is a one-line change in each consumer.
**Alternatives:** (B) Auto-create temporary `equipment` rows tagged `is_adhoc=true` -- pollutes the catalog with one-offs and complicates search/filter logic everywhere. (C) Single sentinel "Ad-hoc" equipment row with name override on the line item -- still needs the same custom_name column, plus a sentinel row feels hacky. Option A wins on long-term cleanliness despite touching 7 files.

### 2026-04-30 -- Session 15
**Decision:** Dashboard catalog search runs client-side on a fetched-up-to-500 result set, mirroring the website's `Catalog.jsx` approach.
**Why:** User wants name+description match AND diacritic insensitivity ("kalove" -> "Kalove"). The catalog is ~150 items so fetching all when a search term is entered is cheap. Server-side filters (category, subcategory, status, in_stock) still run on Postgres for non-search browsing.
**Alternatives:** (B) Enable Postgres `unaccent` extension + a generated `search_text` column with a trigram GIN index -- correct long-term but requires a migration and the generated-column dance for PostgREST to filter on it. (C) Server-side `or(name.ilike.%q%, description.ilike.%q%)` -- gives name+description but doesn't solve diacritics. Option A keeps behaviour consistent across website and dashboard with no DB changes.

### 2026-04-17 -- Session 11
**Decision:** Unified all PDF documents (navrh, finalna zmluva, faktura) to use the same "ZMLUVA O PRENAJME HNUTELNYCH VECI" structure
**Why:** These are all the same rental contract -- just at different lifecycle stages. Having different layouts confused users and made maintenance harder. Invoice number is shown in metadata subtitle, not as a separate document title.
**Alternatives:** Separate PDF generators per document type (original approach); shared template with different titles (rejected by user -- "it's the same contract")

### 2026-04-17 -- Session 11
**Decision:** Multiple final contracts per reservation (partial returns) instead of single finalization
**Why:** Clients sometimes return only some rented items and keep others. A single finalization forced all items to be returned at once. Now each return creates its own final contract with only the returned items, and the reservation auto-completes when all items are returned.
**Alternatives:** Single finalization with "exclude items" option; separate reservations per item group (too complex for user workflow)

### 2026-04-17 -- Session 11
**Decision:** Equipment "Nedostupne" uses existing `status` field (active/inactive) rather than a new boolean
**Why:** The public catalog already filters `.eq('status', 'active')`, so setting status to 'inactive' automatically hides from royalstroje.sk without any portal code changes. No migration needed.
**Alternatives:** New `is_visible` boolean column (would require migration + portal filter update)
